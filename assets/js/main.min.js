const FORMSPREE_ENDPOINT = 'https: if (typeof AOS !== 'undefined') { AOS.init({ duration: 700, once: true, offset: 80 }); } function animateStat(el) { const target = parseFloat(el.dataset.target); const decimals = parseInt(el.dataset.decimals || '0', 10); const duration = 1200; const start = performance.now(); function frame(now) { const t = Math.min((now - start) / duration, 1); const eased = 1 - Math.pow(1 - t, 3); const value = target * eased; el.textContent = decimals > 0 ? value.toFixed(decimals) : String(Math.round(value)); if (t < 1) requestAnimationFrame(frame); } requestAnimationFrame(frame); } const statObserver = new IntersectionObserver((entries) => { entries.forEach((entry) => { if (!entry.isIntersecting) return; const el = entry.target; if (el.dataset.counted) return; el.dataset.counted = '1'; animateStat(el); statObserver.unobserve(el); }); }, { threshold: 0.4 }); document.querySelectorAll('.stat-num').forEach((el) => statObserver.observe(el)); function clearErrors(form) { form.querySelectorAll('.field-error').forEach((el) => el.remove()); form.querySelectorAll('.border-rose-400').forEach((el) => el.classList.remove('border-rose-400')); } function showFieldError(input, msg) { input.classList.add('border-rose-400'); const err = document.createElement('p'); err.className = 'field-error text-rose-600 text-[11px] mt-1'; err.textContent = msg; input.parentElement.appendChild(err); } function validateForm(form) { clearErrors(form); let valid = true; const name = form.querySelector('[name="full_name"]'); if (name && !name.value.trim()) { showFieldError(name, 'Name is required.'); valid = false; } const email = form.querySelector('[name="email"]'); if (email) { const v = email.value.trim(); if (!v) { showFieldError(email, 'Email is required.'); valid = false; } else if (!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(v)) { showFieldError(email, 'Enter a valid email address.'); valid = false; } } const company = form.querySelector('[name="company"]'); if (company && !company.value.trim()) { showFieldError(company, 'Company is required.'); valid = false; } const message = form.querySelector('[name="message"]'); if (message && !message.value.trim()) { showFieldError(message, 'This field is required.'); valid = false; } return valid; } async function submitDemoForm(form) { const honeypot = form.querySelector('[name="_gotcha"]'); if (honeypot && honeypot.value) return; if (!validateForm(form)) return; const btn = form.querySelector('#submit-btn'); const success = form.querySelector('#form-success-msg'); const error = form.querySelector('#form-error-msg'); if (btn && !btn.dataset.defaultHtml) { btn.dataset.defaultHtml = btn.innerHTML; } const defaultBtn = btn ? btn.dataset.defaultHtml : ''; if (success) success.classList.add('hidden'); if (error) error.classList.add('hidden'); if (btn) { btn.disabled = true; btn.innerHTML = '<span>Submitting\u2026</span><span class="material-symbols-outlined animate-spin text-[16px]">sync</span>'; } try { const res = await fetch(FORMSPREE_ENDPOINT, { method: 'POST', body: new FormData(form), headers: { Accept: 'application/json' } }); if (!res.ok) throw new Error('submit failed'); if (btn) btn.classList.add('hidden'); if (success) success.classList.remove('hidden'); } catch (e) { if (error) error.classList.remove('hidden'); if (btn) { btn.disabled = false; btn.innerHTML = defaultBtn; } } }